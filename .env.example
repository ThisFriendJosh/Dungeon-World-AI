# Runtime
NODE_ENV=development

# App data root override (optional). If empty, OS default app-data dir is used.
DWA_DATA_DIR=

# Reserved for future local AI/art service paths
LOCAL_LLM_MODEL_PATH=
LOCAL_IMAGE_MODEL_PATH=
