# services/art (stub)

Offline artwork generation service placeholder.

Planned behavior:
- deterministic asset generation from `{campaignMasterSeed, entityId, assetType}`
- persist `AssetSpec` JSON alongside generated image
- enforce campaign `ArtProfile`
